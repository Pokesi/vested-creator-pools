{
  "name": "hardhat-project",
  "devDependencies": {
    "hardhat": "^2.12.3"
  },
  "scripts" : {
    "compile-solana": "compile_all solana",
    "compile-evm": "compile_all evm",
    "compile": "compile_all all"
  }
}
